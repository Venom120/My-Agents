# Setup — Install My-Agents From Scratch

This directory contains everything a brand-new user needs to install the
**My-Agents + OmniRoute** stack on a clean machine.

> **You do NOT need to clone this whole repository to use My-Agents.** The
> OpenCode and DeepSeek Harness (DSH) presets are loaded directly from
> GitHub by the host application. This `setup/` directory is only for
> users who want to:
>
> 1. Run the **OmniRoute** gateway locally (the AI model router).
> 2. Manage the **six free Engine Combos** that My-Agents routes against.
> 3. Run OpenCode or DSH as a **persistent background service** under
>    systemd (Linux) or as a startup task (Windows).

---

## What you actually need to install

| Component | Required? | What it does |
|---|---|---|
| **OmniRoute** | ✅ Yes | The local AI model gateway. My-Agents subagents talk to OmniRoute, and OmniRoute talks to the actual providers (OpenRouter, Mistral, Groq, etc.). |
| **Python 3.8+** | ✅ Yes (for combo management) | Runs the two Python scripts under `setup/omniroute/`. |
| **My-Agents preset** | ✅ Yes | Loaded into OpenCode or DSH from `github:Venom120/My-Agents#main`. You do **not** clone it manually. |
| **OpenCode** *or* **DeepSeek Harness (DSH)** | Pick one | The host application that runs the agents. You do **not** need both. |
| **systemd service files** | Optional | Auto-start OpenCode / DSH / OmniRoute on Linux boot. |
| **Windows tray launcher (`start-omniroute.vbs`)** | Optional (Windows only) | System-tray launcher for users running Linux inside WSL from Windows. |

---

## Step-by-step guides (pick your host)

The guides are split by host application. **You only need ONE of them.**

| Guide | When to use it |
|---|---|
| [`omniroute/README.md`](omniroute/README.md) | **Always read this first.** It installs OmniRoute, the model gateway that both OpenCode and DSH depend on. |
| [`opencode/README.md`](opencode/README.md) | Use this if your host is **OpenCode**. |
| [`dsh/README.md`](dsh/README.md) | Use this if your host is **DeepSeek Harness (DSH)**. |

Each guide is a copy-pasteable, step-by-step walkthrough with **separate
Linux and Windows sections**.

---

## Quick start (TL;DR)

If you have already read the architecture and just want to boot the
stack on a Linux box with systemd:

```bash
# 1. Install OmniRoute (gateway)
curl -fsSL https://get.omniroute.ai | sh
sudo systemctl enable --now omniroute
export OMNIROUTE_MANAGE_KEY='paste-your-key-here'
export OMNIROUTE_API_KEY='paste-your-inference-key-here'

# 2. Build the six My-Agents Engine Combos
cd setup/omniroute
pip install requests
python3 omniroute_free_combos.py

# 3a. If you use OpenCode:
cd ../opencode
# follow opencode/README.md

# 3b. If you use DSH:
cd ../dsh
# follow dsh/README.md
```

For Windows / macOS / Docker equivalents, see the per-host README.

---

## Repository layout of `setup/`

```text
setup/
├── README.md                          # this file
├── start-omniroute.vbs                # Windows tray launcher (WSL users)
├── omniroute/
│   ├── README.md                      # OmniRoute install + combo guide
│   ├── omniroute_free_combos.py       # builds the 6 My-Agents combos
│   ├── list-models-omniroute.py       # exports models/providers to CSV
│   ├── omniroute.service              # systemd unit (Linux)
│   ├── omniroute_models.csv           # generated by list-models-…
│   ├── omniroute_providers.csv        # generated by list-models-…
│   ├── omniroute_models_raw.json      # generated by list-models-…
│   └── omniroute_providers_raw.json   # generated by list-models-…
├── opencode/
│   ├── README.md                      # OpenCode install guide
│   └── opencode.service               # systemd unit (Linux)
└── dsh/
    ├── README.md                      # DSH install guide
    ├── commands.txt                   # quick reference for dsh CLI
    ├── dsh.service                    # systemd unit (Linux)
    └── settings.yaml                  # DSH model configuration
```

The four `omniroute_*.csv` / `*.json` files are **not** committed; they
are produced by `list-models-omniroute.py` and are listed here only for
reference. The `setup/` folder itself is **excluded** from the OpenCode
plugin loader — it is never copied into the OpenCode package cache (see
`plugin/load-agents.ts`).

---

## Verifying your install

After completing all three steps, you should be able to:

1. Hit `http://127.0.0.1:20128/v1/models` and see a JSON list of
   providers.
2. Hit `http://127.0.0.1:20128/v1/engines` and see the six My-Agents
   combos (`free-coding-deep`, `free-coding-standard`, `free-coding-fast`,
   `free-reasoning`, `free-context`, `free-vision`).
3. Launch OpenCode (or DSH), open the Master agent, and run a small task.
   The Master should report a route, you approve it, and the chosen
   pipeline worker should start executing.

If any step fails, the per-host README has a **Troubleshooting**
section.

---

## Windows users: the tray launcher (`start-omniroute.vbs`)

If you run My-Agents on **Windows** with a Linux distro installed under
**WSL** (the most common setup), the bundled
[`start-omniroute.vbs`](start-omniroute.vbs) gives you a system-tray
icon to start, stop, restart, and toggle autostart for **OmniRoute**,
**OpenCode**, and **DeepSeek Harness** without opening a terminal.

### What the tray does

- Single-instance: launching the VBS twice does not start a second tray.
- Starts Docker Desktop and your WSL distro (with dbus) automatically.
- Reads `systemctl is-enabled SERVICE` to show a checkmark next to
  "Startup" only when systemd is set to autostart the service.
- Clicking "Startup" toggles `systemctl enable` / `systemctl disable`
  for that service. systemd remains the source of truth.
- Live colors: green = running, orange = starting / stopping,
  red = stopped / failed. Refreshes every 2 s.
- Right-click → Exit stops the tray cleanly and releases the mutex.

### Picking your distro (one-time edit)

The tray defaults to a distro named `ArchLinux`. To make it work with
**any** Linux distro (Ubuntu, Debian, Fedora, openSUSE, Kali, etc.):

1. Open `setup/start-omniroute.vbs` in a text editor.
2. Find the line:
   ```vbs
   WSL_DISTRO = "ArchLinux"
   ```
3. Replace `ArchLinux` with the exact name of your distro. To find
   the name, run in PowerShell:
   ```powershell
   wsl -l -q
   ```
   Examples: `Ubuntu`, `Ubuntu-22.04`, `Debian`, `openSUSE-Leap-15`,
   `kali-linux`, `archlinux` (lowercase on some installs).
4. Save the file.

The rest of the script adapts automatically.

### Where to put the file

The VBS is just a Windows Script Host file — it does not need to be
installed. Common places to keep it:

| Location | Why |
|---|---|
| `C:\Users\<you>\Scripts\start-omniroute.vbs` | Clean user-level location, easy to find. |
| Same folder as your cloned My-Agents repo | Keeps everything together if you are reading the source. |
| Anywhere on your PATH or Startup folder | For autostart at logon. |

To auto-start the tray at Windows logon:

1. Press `Win + R`, type `shell:startup`, press Enter.
2. The Startup folder opens. Right-click → New → Shortcut.
3. Browse to your `start-omniroute.vbs` and finish the wizard.
4. On the next logon, the tray icon appears in the notification area.

### What you still need

The VBS only manages systemd services. Before using the tray, make sure
you have:

1. Installed your WSL distro and confirmed `wsl -l -q` lists it.
2. Installed Docker Desktop and enabled WSL integration for your
   distro (Settings → Resources → WSL Integration).
3. Installed **OmniRoute** inside WSL (see
   [`omniroute/README.md`](omniroute/README.md)) and copied
   `omniroute.service` to `/etc/systemd/system/`.
4. Installed **OpenCode** and/or **DSH** inside WSL and copied their
   service files to `/etc/systemd/system/`.

After all four are in place, double-click `start-omniroute.vbs` to
launch the tray. The menu reflects reality; "Startup" toggles
autostart; "Open" jumps to the web UI of each service.

### Switching OpenCode profiles (My-Agents ↔ ECC)

The My-Agents stack ships with a second profile for OpenCode:
**ECC** (Elite Claude Code) — a 68-agent, 286-skill, 94-command
swarm that runs alongside the My-Agents 6-stage pipeline. You
switch between them from the tray:

1. Right-click the tray icon → **OpenCode** → **Profile**.
2. Click **My-Agents** or **ECC**.

The tray:

- Copies the chosen profile to `~/.config/opencode/opencode.jsonc`
  inside WSL.
- For **ECC**: automatically runs `npm install -g ecc-universal`
  and `npm run build` if the package is missing or unbuilt.
- Restarts `opencode.service` and waits up to 30 s for it to come
  back up.
- Updates the checkmark in the menu from a SHA-256 hash of the
  active config (not a string match), so hand-edits are
  detected and shown via an "Unknown active profile" state.

Every step is logged to:

```text
%LOCALAPPDATA%\My-Agents\tray.log
```

The log is truncated to the last 5 MB on every tray launch. Error
pop-ups offer to open the log in Notepad.

The two profile templates are checked into the repo at
[`opencode.my-agents.jsonc`](../opencode.my-agents.jsonc) and
[`opencode.ecc.jsonc`](../opencode.ecc.jsonc) at the My-Agents
repo root. They use the `.jsonc` extension (OpenCode's native format)
and are copied to `profile.<name>.json` inside WSL when deployed. The tray prefers the on-disk templates and falls back
to embedded copies if the repo is not at the expected
`/mnt/d/Github/My-Agents` path.

> **DSH profiles are NOT switched from the tray.** Use
> `dsh plugin --profile web add` in a terminal. See
> [`dsh/README.md`](dsh/README.md).

### User-added plugins survive profile switches

The tray does **not** blindly overwrite the live `opencode.jsonc` with
the chosen template. It reads the live file, separates each entry in
the `plugin` block into:

- **Base** entries that match the target template's `plugin` block
  (deep structural compare).
- **User extras** — every other entry.

It then writes a merged file: `targetBase + userExtras` for `plugin`,
plus a merge of the `provider` block that keeps any non-template
providers the user added. The previous live file is backed up to
`opencode.jsonc.bak` first, so a hand-edit that the merge misreads can
be recovered with `cp ~/.config/opencode/opencode.jsonc.bak
~/.config/opencode/opencode.jsonc`.

Practical effect: `opencode plugin add foo` while on the My-Agents
profile → switch to ECC → switch back to My-Agents → `foo` is still
in the active `opencode.jsonc`. Every preserved extra is logged to
`%LOCALAPPDATA%\My-Agents\tray.log` as
`merge: kept N user plugin(s): …`.

Edge cases that are handled and logged:

- The base of the live file drifted from the template (hand-edit
  or a fork) — the user's version is kept and the status tick
  shows "Unknown active profile" until the template is updated.
- A `plugin` entry is malformed JSON — the tray aborts the switch
  and shows the parse error.
- The `opencode.jsonc.bak` write fails (read-only mount) — the
  switch is aborted and the old live file is left in place.
